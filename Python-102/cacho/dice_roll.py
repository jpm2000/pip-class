import random
import options

random_dice = random.choice(options.options)
print(random_dice)
'''
user1 = {
  'dice 1': random.choice(options.options),
  'dice 2': random.choice(options.options),
  'dice 3': random.choice(options.options,
  'dice 4': random.choice(options.options),
  'dice 5': random.choice(options.options)
}

print(user1)
'''
